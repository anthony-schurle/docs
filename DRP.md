Attribute-Level:
Since the authors use JL-transforms to get that optimal additive error, would we lose that efficiency if we allowed users to have different $\epsilon$ levels for different features? Would we be forced back into a suboptimal multiplicative error like $d \cdot \log(1/\beta)$?

